# Decision Log

Tracks *why* a call was made, not *what* was added (see `CHANGELOG.md` for that). One dated entry per meaningful decision. Newest at bottom.

## 2026-08-10

**Project title finalized as "Broadsheet & Rapier".**
"The Hollow Atlas" was an earlier working title, replaced. Engine identity finalized as "Thornwall" (not "thornwall-engine").

**Princess Trainer added as a UI/UX & interaction-mechanics inspiration, kept separate from narrative inspirations.**
It's a game, not a book, and has no bearing on tone, dialogue, or lore — only on interaction structure (stat-driven menus, time-slot scheduling, discrete location transitions, modal overlays). Placed in its own section of `narrative-inspirations.md` rather than merged into the Section 1/2 priority hierarchy, to avoid it leaking into narrative tone generation.

**Actor faction affiliation modeled as `factionIds: string[]` rather than a single `factionId`.**
An Actor can plausibly belong to more than one faction or have shifting allegiances; a single optional field couldn't represent that.

**District/POI given optional `controllingFactionId` and `factionInfluence` fields.**
Needed for locations to plausibly be contested or influenced by multiple factions rather than only cleanly owned or neutral.

**Territory deferred — no schema, no content, no reference field on Settlement.**
Not needed until macro-world/regional travel is actually implemented; adding it now would be unused scaffolding.

**`COMMAND_NEXT_DAY` made internal-only.**
It's a derived consequence of `COMMAND_ADVANCE_SHIFT` rolling past `NIGHT`, not an independent player action. Exposing it as a dispatchable UI command would let it be triggered out of sequence.

**`importSave` validates against `PlayerStateSchema` before writing to state, but without version migration logic.**
Protects against corrupted or hand-edited save files causing a crash. Full migration handling deferred until the state shape is stable enough that migrations are actually needed.

**Tailwind CSS v4 used instead of v3, changing the original setup plan.**
`npx tailwindcss init -p` no longer works in v4 — the CLI/init flow was removed upstream. v4 integrates via the `@tailwindcss/vite` plugin directly, no `postcss.config.js` or `tailwind.config.js`. Chosen over pinning to v3 because v4 is the current supported line going forward.

**`docs/system-rules.md` split into `docs/game-design-spec.md` (engine-agnostic) and `docs/web-implementation.md` (React/TS/Zustand-specific).**
The project may migrate off web technology (e.g. to Godot) in the future. Mixing domain rules (world clock, currency, content model) with implementation details (TypeScript interfaces, Zustand persistence, Tailwind classes) in one file meant a future migration would require manually untangling which parts still apply. Splitting means `game-design-spec.md` should survive a migration unchanged, and `web-implementation.md` is the only file that needs full replacement.

**Execution plan scoped explicitly as "Phase 0 — Technical Scaffold".**
The original plan covered engine types, schemas, store, starter content, tests, and UI shell — but no actual gameplay mechanics (minigame resolution, reputation effects, economy balance). Left unscoped, an implementer would have had to invent those to make anything "playable." Scaffold is now bounded to proving the architecture with inert content; gameplay mechanics require their own spec before implementation continues past it.

**`COMMAND_NEXT_DAY`'s internal-only status enforced structurally, not just by convention.**
The earlier decision (above) said it shouldn't be exposed as a UI action, but nothing stopped code from dispatching it directly. `applyCommand` now throws if `COMMAND_NEXT_DAY` is dispatched, and the day-rollover logic was moved into a private function called only by the `COMMAND_ADVANCE_SHIFT` handler. Prevents the derived-consequence-only guarantee from silently eroding as more UI code starts calling `dispatchCommand`.

**Content-derived data (POI `costShifts`, Endeavor phase IDs, `unlocksNodesOnComplete`) passed via command payload instead of looked up by the store.**
`src/engine/` may never import `src/content/`, but several command handlers (`COMMAND_MOVE_TO_POI`, `COMMAND_START_ENDEAVOR`, `COMMAND_ADVANCE_ENDEAVOR_PHASE`) need values that only exist in content JSON. Rather than weakening the decoupling rule, the caller — which does have content access — supplies those values as part of the payload. Documented in `web-implementation.md` §3 so Phase 6 (UI) follows the same pattern instead of reaching into content from the engine.

**`COMMAND_ADJUST_CURRENCY` auto-normalizes bronze→silver→gold on every adjustment; no negative-balance protection added.**
`game-design-spec.md` §5 states the 20:20 conversion rate but doesn't say whether it's applied automatically; auto-normalizing was inferred from `execution-plan.md` Phase 5 mentioning a "currency conversion boundaries (400 bronze = 1 gold)" store test. Negative-balance handling (e.g. blocking an unaffordable purchase) was deliberately left out — that's part of the economy-balance open design gap, not a structural/type concern, and inventing it now would preempt that spec.

**`worldClock.weather` added to `PlayerState` as a display-only field, no weather mechanic invented.**
`execution-plan.md`'s Phase 6 spec for `WorldClockHud` requires showing weather, but `PlayerState` had no weather field at all and `game-design-spec.md` §4 never defined its representation. Rather than skip the HUD element or invent a weather-change mechanic, added a minimal `Weather` enum (`CLEAR | RAIN | FOG | STORM`, mirroring the existing `Shift`/`Season` const-array pattern) purely so the field exists and can render. It starts at `CLEAR` and nothing currently changes it — no `CommandType` sets weather, since how/when it should change is undefined. Also promoted `Season` to the same named-const-array pattern as `Shift` while touching this code, for the same drift-prevention reason `Shift` was promoted earlier. The "nothing updates it" gap is now formalized as `game-design-spec.md`'s Open Design Gap #5, rather than only living in this log entry — so an implementer scanning gaps (not decisions) still finds it.

**`MinigameLauncherPayload.config`'s eventual shape (a discriminated union) logged as a formal open gap, not just fixed as `unknown` and left implicit.**
Changing `config: Record<string, any>` to `Record<string, unknown>` (see below) closes the immediate lint/strictness violation, but `unknown` is still a placeholder, not a real type — it says nothing about what a `DUEL` config vs. a `FISHING` config actually needs. Recorded in `game-design-spec.md`'s minigame-mechanics gap entry that once mechanics are specified, `config` should become `DuelConfig | LockpickingConfig | FishingConfig | DiceConfig` keyed off `type`, so the follow-up isn't lost between now and whenever that spec gets written.

**`config: Record<string, any>` changed to `Record<string, unknown>` in both `engine/types/index.ts` and `web-implementation.md` §4's code sample.**
`any` violated `CONTRIBUTING.md`'s "no any" rule (caught by `npm run lint`, not `tsc`, which doesn't flag it) — but it had been copied verbatim from `web-implementation.md` §4's own spec text in Phase 1, so fixing the code without fixing the doc would have made them diverge again. Both changed together, plus a comment in `index.ts` marking it a placeholder pending the minigame-mechanics spec.

**Component testing (`@testing-library/react` + `jsdom`) added to the tech stack as a standing rule, not a one-off.**
Phase 6 shipped six UI components with zero automated coverage — verified only via a manual Playwright/browser pass, which doesn't run on every future change the way `npm run test` does. Rather than re-litigate "does the DoD apply to UI" every phase, added the two packages to `CLAUDE.md`'s tech-stack boundary and wrote the rule directly into `CONTRIBUTING.md`: structure/behavior component tests (jsdom) are required alongside logic tests from now on; a full visual/real-browser pass (Playwright) is reserved for deliberate milestones via the new `ui-visual-check` skill, not routine per-phase work — screenshots are expensive and slow to be the default verification method for every small change.

**Dialogue branching logged as an open gap rather than left undiscovered.**
`Actor.initialDialogue` is one static string per Actor. The state needed to vary it (`activeEndeavors`, `unlockedClues`, `reputation`) already exists in `PlayerState`, which makes it tempting to wire up "if phase X, show line Y" ad hoc the next time an Actor needs more depth. Recorded as `game-design-spec.md` Open Design Gap #6 instead, since the actual mechanism (priority order between conditions, how much branching, authoring format) isn't specified and shouldn't be invented mid-feature.

**Engine UI components take content-derived data as props; only `App.tsx` imports `src/content/` directly.**
`web-implementation.md` §3 bars `src/engine/` from importing `src/content/`, but `WorldNavigationView` and `NodeInteractionCanvas` need settlement/district/POI/actor names and descriptions to render anything. Rather than weaken the boundary, `App.tsx` (outside `src/engine/`) does the content lookups and passes resolved data down as props — the same pattern already used for content-derived command payloads, applied to rendering.

**Actor-selection state (which NPC's dialogue is showing) kept as local React state, not added to `PlayerState`.**
It's view state — which panel of an already-open POI you're looking at — not something that needs to survive a reload or export. Adding it to `PlayerState`/`PlayerStateSchema` would have meant inventing a new persisted field with no gameplay meaning yet.

**"Leaving a POI" reuses `COMMAND_MOVE_TO_DISTRICT` instead of adding a new command.**
`COMMAND_MOVE_TO_DISTRICT` targeting the current `districtId` already clears `poiId` at zero shift cost, which is exactly "step back out to the district view." Adding a dedicated `COMMAND_LEAVE_POI` would have duplicated existing, already-tested behavior.

**`MinigameOverlay.tsx` added to the component directory tree; it wasn't in the original `web-implementation.md` §7 list.**
`execution-plan.md`'s Phase 6 section requires rendering the minigame modal overlay, but the five named components in §7 didn't include one for it. Added as its own file (full-screen modal, `activeMinigame`-driven, dispatches `COMMAND_RESOLVE_MINIGAME` with a hardcoded victory/defeat choice — no mechanic logic) rather than folding it into an existing component, and updated §7's file list to match.

**Testing moved from a dedicated end-phase to alongside implementation.**
Phase 3's command/store logic (shift rollover, currency conversion,
reputation clamping) shipped and was approved across two phases with
zero behavioral test coverage — only type-checking. Deferring all
tests to Phase 5 meant real logic bugs could sit undetected for
multiple approval cycles. Definition of Done now requires tests
alongside any new logic in the same phase it's introduced.

**CI Node version bumped from 20 to 24, matching local.**
jsdom 28's bundled undici (8.0.3+) requires `node:worker_threads.markAsUncloneable`,
added in Node v21.0.0. CI pinned to Node 20 caused all 6 component test files to
fail at worker startup (not a test failure — they never ran). Local Node 24 has
the API and was unaffected, which is why this wasn't caught until CI ran it.
Aligning versions everywhere going forward avoids this class of "works locally,
fails in CI" gap.

**Node version centralized into `.nvmrc`, rather than just fixing the CI number.**
Bumping `ci.yml`'s hardcoded `node-version: '24'` (above) fixed the immediate
failure but left two separately maintained copies of the same fact — nothing
stopping them from silently drifting apart again the next time either one
changes. Added `.nvmrc` (`24`) as the single source of truth: `ci.yml` now
reads `node-version-file: '.nvmrc'` instead of a hardcoded value, and
`package.json`'s `"engines": { "node": ">=24" }` makes `npm install` itself
warn locally on a mismatch. README points at `.nvmrc` so a human setting up
the repo sees the same requirement, not a fourth copy of the number.

**Currency borrow-down implemented as a single total-value reconstruction, not per-denomination borrowing.**
`COMMAND_ADJUST_CURRENCY` previously only carried up (bronze→silver→gold) and had no way to go the other direction, so a loss bigger than the bronze on hand would just leave a raw negative field. Rather than hand-write borrow logic per denomination (bronze short → break a silver → still short → break a gold → ...), the handler now converts the *entire* current balance to a single bronze-equivalent integer, applies the signed delta, clamps at 0 (the hard floor), and re-splits that one number back into gold/silver/bronze. Carry-up, borrow-down, and the negative-balance floor all fall out of the same three-line function instead of three separate code paths — and it's the same formula already used to compute `currenciesToBronzeEquivalent` for the dice wager ceiling, so there's one canonical definition of "how much money does the player have," not two.

**Dice minigame mechanic: 2d6, even/odd, 2x payout on win — chosen for simplicity/testability/transparent odds, not balance.**
Given four minigame types were completely unspecified, DICE was the one to resolve first because a pure coin-flip-shaped mechanic (parity of a 2d6 sum) has no skill component, no difficulty curve, and no hidden odds to get wrong — the player can mentally verify "even wins" is roughly 50/50 without trusting the implementation. `game-design-spec.md` §9 records this explicitly as a deliberately minimal first mechanic, not a template future minigames must copy.

**Mara Venn's `factionIds` corrected to `["faction_wagering_ring"]` only — `faction_city_watch` removed, not kept alongside it.**
The original starter content gave her City Watch affiliation as a placeholder; wiring her into an actual gambling loop this phase meant she needed to plausibly belong to the syndicate running it instead. Single-faction rather than dual (`factionIds` supports multiple) because there's no narrative reason yet for her to belong to both a law-enforcement faction and a gambling ring — adding both would have papered over the correction rather than making it. Her `title` ("City Watch Sergeant") and `description` were deliberately left untouched (out of scope for this correction), which now reads as narratively inconsistent with her faction — flagged for a follow-up content pass, not silently fixed.

**Player starts with a small currency purse (50 bronze-equivalent) instead of zero.**
Found via the browser verification pass, not requested in the spec: `initialPlayerState.currencies` was `{0,0,0}` from Phase 3, which made the new Gamble action permanently disabled on a fresh save — "Not enough coin" with no way to ever afford the 5-bronze minimum wager. A phase whose stated goal is "the first real playable loop" isn't actually playable if the loop is unreachable from the start. Picked a modest placeholder (`{gold:0, silver:2, bronze:10}`) rather than 0 or something large — explicitly flagged as a placeholder tied to the economy-balance open design gap, not a balanced number.

**Dice wager stays in sync with `activeMinigame` by re-dispatching `COMMAND_START_MINIGAME` on every stepper change, instead of adding a new command.**
The wager is chosen *after* the minigame launches (the player can adjust the stepper before throwing), but `COMMAND_RESOLVE_MINIGAME` needs `onSuccessCommands`/`onFailureCommands` to already contain the correct `COMMAND_ADJUST_CURRENCY` amount when it fires. Rather than add a `COMMAND_UPDATE_MINIGAME_CONFIG` or let `DiceGame` bypass the payload pattern and dispatch currency changes directly, `setWager` just re-sends the whole `COMMAND_START_MINIGAME` payload with the new wager baked into both command lists — `COMMAND_START_MINIGAME`'s handler already does a full overwrite of `activeMinigame`, so this needed zero store changes.

**Endeavor completion (`phase_confront_the_buyer`, which has no `nextPhaseOnSuccess`) marked via `COMMAND_UNLOCK_CLUE` with a dedicated clue ID, not a new command or state field.**
`activeEndeavors` entries have no `isComplete` flag, and this phase's instructions explicitly ruled out a schema change. `unlockedClues` was already exactly "a set of durable flags the player has reached" — reusing it for `clue_broadsheet_case_closed` cost nothing new and matches the session's running preference for reusing an existing primitive over inventing a command for a one-off need (see the `COMMAND_MOVE_TO_DISTRICT`-as-"leave POI" decision above).

**`NodeInteractionCanvas` gained a generic `actions?: NodeInteractionAction[]` prop instead of hardcoding Gamble/Pay-off-the-buyer into the component.**
Both actions are specific to one POI and one Endeavor phase — genuinely content-specific, not something `src/engine/components/` should know about by name. `App.tsx` builds the action list (which buttons, their labels, their `onClick` logic) and passes it down, consistent with every other content-derived prop the component already takes.

**`COMMAND_CANCEL_MINIGAME` added — a genuinely new command, unlike the clue-marker case below.**
Found via manual testing: a player who opens the Dice modal with too little to afford `MIN_WAGER` had no way to close it. `COMMAND_RESOLVE_MINIGAME` always applies `onSuccessCommands` or `onFailureCommands` — there was no existing command that clears `activeMinigame` without a win/lose consequence, and no existing primitive to repurpose for one (unlike "mark an endeavor complete," this need has no adjacent state that already means "leave without effect"). Kept minimal: no payload, clears `activeMinigame`, nothing else. `DiceGame`'s "Leave" button dispatches it and stays enabled even when "Throw" is disabled for insufficient funds.

**Reversed: endeavor completion no longer uses `COMMAND_UNLOCK_CLUE` as a marker — reverts the decision two entries above.**
On review, repurposing `unlockedClues` (meant for narrative discoveries) as a mechanical "already paid" flag was the wrong call — it solved a real problem (preventing repeat payment) by overloading a system that means something else. Removed the dispatch entirely rather than finding a different existing field to repurpose (state fields like `logHistory` or `unlockedNodes` would have had the same semantic-misuse problem, just relocated). "Pay off the buyer" now only dispatches its `COMMAND_ADJUST_CURRENCY` cost and stays visible/clickable for as long as `activeEndeavors[endeavorId].currentPhaseId === 'phase_confront_the_buyer'` — accepting that it's repeatable (each click cost real currency) as a deliberate simplification, the same category as the already-accepted no-anti-grinding reputation gap, rather than engineering real one-time-completion tracking without a schema change.

**Mara Venn's `title` and `description` updated to match her corrected faction, closing the gap flagged two entries above.**
Left as "City Watch Sergeant" / "A City Watch sergeant who..." when only `factionIds` was corrected, which just relocated the inconsistency from the faction field to the title/description instead of removing it. New title "Wagering Ring Regular"; description reframed around her being a trusted fixture at the tavern's back tables rather than an off-duty watch officer — keeps the "she hears things" Garrett-informant framing that made her useful as a source, drops the watch affiliation entirely.

**Fallen London, OGame, and BiteFight added as gameplay-systems inspirations — a new category, distinct from narrative/atmosphere/UI.**
`narrative-inspirations.md` previously covered narrative tone (§1), atmosphere (§2), and UI/interaction mechanics (§3) — nothing about repeatable-activity or progression *design*, even though the dice gambling loop just became the first real instance of that category with no reference to design against. Added as §4, explicitly marked as reference material "not yet adopted into concrete mechanics," not a spec — generalizing it into an actual system is deferred as `game-design-spec.md`'s Open Design Gap #9. Fallen London's entry includes a design-principles subsection sourced from a practitioner discussion (intfiction.org) on prototyping a similar Quality/Storylet system, kept alongside the game's own systems description since the principles are the more directly actionable part for future design work. Sensory palette renumbered from §4 to §5 to make room.

**Sound effects split into two trigger mechanisms — logic-driven (dice win/lose) and content-driven (`entrySoundAsset`) — rather than one unified system.**
The two cases genuinely have different owners: dice win/lose is a game-logic outcome with no content-JSON representation to hang a schema field off of, while District/POI entry is exactly the kind of per-node data `imageAsset` already models. Forcing both through a single mechanism would have meant either inventing fake content fields for the dice outcome or hardcoding a schema-driven lookup inside component logic that has nothing to do with content. `entrySoundAsset` (not `ambientSoundAsset`) was chosen deliberately: this phase is scoped to one-shot SFX triggered by a specific moment (arriving at a place), not looping ambience — naming it `ambientSoundAsset` would imply a persistent/looping sound bed that doesn't exist yet and would need different lifecycle handling (start on enter, stop on leave, no double-triggering) that's explicitly out of scope. Established now, in the same phase as the dice win/lose wiring, rather than deferred, because both need the same shared fail-silent utility — building that utility to serve only one caller would have been an artificial narrowing, and proving it against two independent trigger shapes (an outcome branch and a content field) in one pass is a stronger validation than doing them in separate phases. This also finally builds against the `audio/` folder that has sat referenced-but-unused in the directory blueprint since early in the project — the same category of "documented but never built" gap this session has closed before (e.g. `minigames/index.ts`, the content-integrity test).

**District's `entrySoundAsset` fires once on initial app mount, not from `onLeave` (the only place `COMMAND_MOVE_TO_DISTRICT` currently dispatches).**
The original plan wired it to `onLeave`, reasoning that's the only existing `COMMAND_MOVE_TO_DISTRICT` call site. On review this was wrong: `onLeave` fires when stepping out of a POI back into the *same* district the player never actually left — playing an "entering this district" sound there would misrepresent "leaving a building" as "arriving somewhere new." This was a semantic mismatch, not a technical limitation — nothing stopped the code from working, it just would have meant the wrong thing. A `useEffect` on mount against the currently-resolved district is the one place in the current single-district world that's honestly "the player has entered this district," and it keeps working unchanged once real district-to-district travel exists later (a future "move to district" action calls the same trigger function; the mount effect only needed a code comment flagging that its empty dependency array will need revisiting at that point, since `district` is a static import today). Dice win/lose and the POI entry sound (`onSelectPoi`, a real per-POI selection event) were not affected by this reconsideration — only the district-entry trigger point changed.

**`resetProgress` added as a store action (`usePlayerStore`), not an ad hoc `setState` call from `ManagementDrawer`.**
Mirrors `exportSave`/`importSave` exactly — both already live in the store as the sanctioned place for whole-state-replacement operations, and `resetProgress` is conceptually identical to `importSave` targeting a fixed value (`initialPlayerState`) instead of a parsed file. Keeping it in the store instead of the component means `ManagementDrawer` stays a thin consumer, consistent with every other store-native component in `src/engine/components/`. Dev-only gating (`import.meta.env.DEV`) and a two-click in-UI confirm step (not a native `window.confirm`, which is harder to test and less consistent with the game's own visual language) live in the component, since those are UI/UX concerns, not state-management ones.

**Referential-integrity checks in `content-integrity.test.ts` scoped to exactly three relationships (+ their reverses), not every reference-shaped field.**
`Actor.factionIds`, `POI.actorIds`/`Actor.poiId`, and `District.poiIds`/`POI.districtId` are checked. `controllingFactionId`, `factionInfluence` keys, `District.settlementId`, and `Endeavor.unlocksNodesOnComplete` are equally reference-shaped and equally checkable in principle, but were left out of this pass — extending referential-integrity checking to *every* reference field the schemas contain wasn't what was asked, and silently over-delivering would have made the actual scope of coverage less legible than stating it explicitly (which the test file's own comment now does). Extending further is a small, well-understood follow-up whenever it's actually needed, not a design problem to solve now.

## 2026-08-11

**Content loading in `App.tsx` routed through each schema's `.parse()` (`src/contentLoader.ts`'s `loadContent`) instead of consuming raw static `import`s directly — a root-cause fix, not a per-field patch.**
Found during the dialogue-branching phase: `DialogueChoiceSchema.commands` is declared `z.array(StateCommandSchema).default([])`, but that default is only ever applied when data is actually run through `.parse()`/`.safeParse()`. `App.tsx` loaded every content file (settlements, districts, POIs, actors, endeavors, dialogues) via plain `import x from "./content/....json"`, which returns the JSON exactly as authored — a field omitted in the raw file stays `undefined`, not `[]`. `content-integrity.test.ts` and `schemas.test.ts` both only ever exercise the *parsed* shape (they call `schema.safeParse(rawData)` themselves), so neither test file can distinguish "this field will be `[]` at runtime" from "this field will be `undefined` at runtime" — both look identical to a passing test. This shipped as a real bug: `dialogue_mara_venn.json`'s `choice_press_for_lead` omitted `commands`, and `COMMAND_SELECT_DIALOGUE_CHOICE`'s handler crashed (`commands is not iterable`) the first time a real player reached it, despite `npm run test` being fully green. Caught only by the manual browser reachability check (`docs/feature-workflow.md` Category A/E), not by any automated check.

The first fix (a defensive `commands ?? []` in the command handler) closed that one instance but was explicitly called out as treating a symptom — the same exposure exists for every other `.default(...)` field any content schema declares (`DialogueNodeSchema.choices`, `PoiSchema.costShifts`, `ActorSchema.factionIds`, and any future one), and patching each usage site as it's individually discovered doesn't scale and doesn't get found until something crashes in front of a player. Root-caused instead: `src/contentLoader.ts`'s `loadContent(schema, data, label)` runs every content import through `schema.safeParse`, throwing a clear, labeled error immediately on failure — matching `content-integrity.test.ts`'s existing strictness, now enforced in production too, not only at test time. Every component downstream of `App.tsx` can now assume any content-derived prop is the fully validated, defaulted shape its schema promises; the now-redundant `target?.costShifts ?? 0` fallback at the one live call site (`COMMAND_MOVE_TO_POI`'s payload) was simplified accordingly, since the only remaining reason for the `?:` there is "no matching POI found," not "the field might be missing."

Audited every content schema for other `.default(...)` fields as part of this fix: `Actor.factionIds` (default `[]`) and `Poi.costShifts` (default `0`) were the other two live instances; neither had a production consumer that would crash on `undefined` at the time (no code read `Actor.factionIds` outside tests, and `costShifts` already had a defensive `?? 0`), but both are now covered by the same loading-boundary fix regardless, so no per-field patch was needed for them. `DialogueRequirementSchema`'s `requires` field on a dialogue choice was checked separately and confirmed *not* exposed to this class of bug: it's `.optional()` with no `.default()`, so an omitted field is `undefined` whether parsed or raw — `evaluateDialogueRequirement`'s first line already treats `undefined` as "no requirement, always available," and that was true before this fix too.

Deliberately scoped to content-JSON loading only — `dispatchCommand`'s own input (commands `App.tsx`/`DialogueOverlay.tsx` build as plain TypeScript object literals) stays unvalidated at runtime, exactly as already scoped out in the dialogue-branching design doc and `web-implementation.md`'s `StateCommandSchema` note. Fixing that boundary is a separate, larger change touching every dispatch call site in the app, not a natural extension of this one.

**`npx tsc --noEmit` (bare) has been silently checking zero files all project long — fixed to `npx tsc -b --noEmit` everywhere it's used.**
Discovered by accident: `npm run build` (which runs `tsc -b && vite build`) failed on a genuine, pre-existing type error in `minigames.test.ts` that had never once surfaced across many phases of `npx tsc --noEmit` being run and reported clean, including in CI's "Type check" step. Root cause: `tsconfig.json` at the repo root only has `"references"` to `tsconfig.app.json`/`tsconfig.node.json`, no `"files"`/`"include"` of its own — bare `tsc` doesn't follow project references without the `-b` (build) flag, so it was compiling nothing and trivially "succeeding" every time. `npm run lint` didn't catch it either (no type-aware linting configured — confirmed by checking `eslint.config.js` for `parserOptions.project`, absent). This means the type error sat undetected through `tsc`, `lint`, and `test` alike — only a real production build surfaces this class of mistake. Fixed the command in `ci.yml`, `CONTRIBUTING.md`, and the `verify-phase` skill; fixed the one latent error it found (a payload cast in `minigames.test.ts`). This is exactly a category-D risk (`docs/feature-workflow.md`) — an environment/tooling assumption, true nowhere it was actually exercised until something finally exercised it.

## 2026-08-12

**`MinigameLauncherPayload.config` turned into a real discriminated union (`DiceConfig | DuelConfig` for the two resolved types, a loose bag for the two unresolved ones), rather than waiting for all four minigame types to be defined.**
Open Design Gap #1 originally framed this as an all-or-nothing move once `DUEL`/`LOCKPICKING`/`FISHING` were all specified. Doing it now, with only DICE+DUEL resolved, was still correct: TypeScript's discriminated unions work member-by-member, so `LOCKPICKING`/`FISHING` can keep their `Record<string, unknown>` branch unchanged until their own specs land, with zero disruption to DICE/DUEL's typed branches when that happens. `DistanceState` lives in and is re-exported from `duel.ts` (its natural home, mirroring `dice.ts` owning `RandomSource`/`DiceRollResult`) rather than being duplicated in `types/index.ts`.

**`DuelContext` bundles `player`/`opponent`/`distance`/`lastPlayerAction`/`playerReputation` as one object instead of positional parameters — a deliberate future-proofing choice, not incidental.**
The brief for this phase explicitly asked for this shape so that a later addition (equipped weapon informing poise or opponent AI, `game-design-spec.md` Open Design Gap #11) becomes a context-field addition, not a signature change to `evaluateDuelTurn`/`chooseOpponentAction`. `playerReputation` mirrors `PlayerState.reputation`'s exact shape rather than a narrower "just the wagering-ring number" field, so a future action could read a different faction/actor's reputation without widening the type again.

**Taunt's reputation check hardcodes `faction_wagering_ring` inside `duel.ts`, rather than making the target faction part of `DuelConfig`.**
This is explicitly the one worked example the brief asked for ("this first duel only... not a general taunt system"), so generalizing it into an authorable config field would have built a system nobody asked for yet and no second instance to validate the shape against. Revisit if/when a second duel wants a Taunt tied to a different faction.

**Guard-break: a combatant whose poise is already 0 takes bonus energy damage (`GUARD_BREAK_BONUS_DAMAGE`) from a landed Thrust/Dirty Trick — added during plan review, not in the original brief.**
The original brief left poise reaching 0 as purely informational (feeding `chooseOpponentAction`'s heuristics and the UI only). On review, poise hitting 0 with no mechanical consequence at all felt like a missing payoff for a whole stat track — an open guard being a real opening is a natural, minimal addition, not scope creep, since it only fires on a state (poise 0) the original design already tracked. Evaluated off the *pre-turn* poise value specifically so a Dirty Trick's own poise-drain can't retroactively bonus its own damage in the same turn, and never applies to riposte counter-damage (a `PARRY_RIPOSTE` effect, not a `THRUST`/`DIRTY_TRICK` hit). Flagged as a placeholder constant (15) alongside every other duel number, same as `dice.ts`'s wager constants.

**Mutual-knockout tie-break resolves as `PLAYER_DEFEAT`, and `chooseOpponentAction` never selects `TAUNT` — both are placeholder/first-mechanic choices, not considered design.**
Neither was specified in the brief. Checking `player.energy <= 0` before `opponent.energy <= 0` was an arbitrary pick to make the outcome function total (something has to win a same-turn double-KO); Taunt only ever being player-initiated follows directly from its reputation check being meaningful only for the player's own standing, not a general mechanic the opponent AI should reach for.

**Rapier Duel shipped engine-only this phase — no POI/NPC dispatches `COMMAND_START_MINIGAME` with `type: "DUEL"` yet.**
The dice minigame's own history (`docs/features/feature_dice_minigame.md`) shipped fully built and tested but unreachable from a fresh save, caught only by manual testing after the fact. Rather than risk repeating that, or invent an opponent NPC/POI not asked for, this was made an explicit, logged decision instead: per `feature-workflow.md` §2 stage 3, content-wiring is its own later Content/Adventure-spec phase, sequenced after the engine capability it depends on. `docs/features/feature_rapier_duel.md`'s Reachability section names this directly so it reads as a decision, not an oversight.

**`initialPlayerState.inventory` seeded with `item_rapier` ×1 — the first non-empty starting inventory, flavor-only this phase.**
Existing tests (`commands.test.ts`'s `COMMAND_ADD_ITEM` test, `playerStore.test.ts`'s initial-state shape assertion) assumed an empty starting inventory; updated in place rather than left broken, since an empty inventory was never a documented invariant, just an accident of `inventory` never having a starter item before. Doesn't gate Thrust or any other duel action, and poise isn't derived from it yet — logged as `game-design-spec.md` Open Design Gap #11 so the intended future connection (a better rapier raising poise) isn't lost.

**Item made a real content type (`ItemSchema`, `src/content/items/`), not a special case bolted onto `PlayerState.inventory`.**
`item_rapier` existed only as a bare string id with no display data, so the inventory tab showed the raw id. Rather than hardcode a name/description/image lookup table somewhere ad hoc, Item follows the exact same `BaseNodeFieldsSchema.extend(...)` + `loadContent` + content-integrity-glob pattern every other entity already uses — Settlement/District/POI/Actor/Faction/Endeavor/Dialogue all work this way, and an Item is not meaningfully different from those in terms of "how does content get validated and loaded," only in what fields it needs.

**`ItemSchema.imageAsset` overridden to be required, unlike `BaseNodeFieldsSchema`'s optional default.**
Every other content type can go without an image (a POI or Actor with no art is still fully functional). An inventory item without a visible representation defeats the purpose of adding Items at all — the user's request was specifically for a visible name/description/image, not a data record. Every other field on `ItemSchema` (`id`, `name`, `description`, `isUnlocked`) is inherited unchanged.

**`stackable: boolean` added to the Item schema now, with zero enforcement anywhere.**
The user asked for the stackable-vs-unique distinction to exist in the data model before more items get authored, but nothing today would ever add a second copy of any item — there's no second rapier, no crafting, no loot drop. Building enforcement logic (reject/cap a duplicate unique item) ahead of a real case to test it against would be guessing at a policy nobody's asked for yet (what should happen — silently ignore the second copy? Replace it? Error?). Logged as `game-design-spec.md` Open Design Gap #12 instead, alongside the closely related "no equipped-item concept" gap from #11 — both need their own spec once a second item (or the rapier-swapping feature) actually motivates one.

**Asset paths resolved against `import.meta.env.BASE_URL` at the point they become a real `src`/`Audio()` call, via a new shared `resolveAssetUrl` helper — not by rewriting every content file's `imageAsset`/`entrySoundAsset` to be base-aware.**
Found by adding the first real asset file (`item_rapier`'s artwork): `vite.config.ts` sets `base: '/broadsheet-rapier/'` for GitHub Pages, but every content-authored path is a literal root-relative string (`/content/assets/...`), and nothing resolved it against the configured base — so it 404'd under both `npm run dev` and the production build. This was invisible for the entire project so far because every image asset was already showing the MISSING placeholder for an unrelated reason (no file had ever existed), so a wrong URL and a genuinely missing file looked identical. Fixed at the two chokepoints where a path becomes an actual request — `AssetFallback.tsx` (images/audio-with-controls) and `playSound.ts` (fire-and-forget SFX) — rather than teaching every content author to bake `BASE_URL` into their JSON, which would also break the moment the base path ever changes again. The MISSING placeholder's label still shows the original authored path, not the resolved URL, so content authors keep debugging against what they actually wrote.

**GitHub Pages deployment uses the modern Actions-based flow (`actions/upload-pages-artifact` + `actions/deploy-pages`), not a `gh-pages` branch push.**
No `dist/`-to-branch tooling (e.g. the `gh-pages` npm package) was added — the Actions-native flow needs no extra dependency, no separate branch to keep in sync, and is what GitHub itself now recommends. Requires one manual one-time step outside this repo's files: Settings → Pages → set the deployment source to "GitHub Actions" (can't be done from a commit; flagged to the user rather than silently assumed done). `vite.config.ts`'s `base: '/broadsheet-rapier/'` was already correct for a project-page URL (`gsoster.github.io/broadsheet-rapier/`) — confirmed by inspecting a local `npm run build`'s output before wiring up the workflow, rather than assuming it and finding out from a broken deployed site.

**`DialogueOverlay`'s new dismiss button is labeled "Close", not "Leave" (unlike Dice's "Leave"/Duel's "Flee").**
A generic "leave the modal without picking anything" button already exists in two other minigame UIs, both worded "Leave"/"Flee" — but Mara Venn's own dialogue content already has a real in-conversation choice whose text happens to be "Leave" (ending the conversation). Reusing that word for the new always-present dismiss button would make `screen.getByText("Leave")` (and a real player scanning the screen) ambiguous between "end the conversation via a real choice" and "back out without choosing." `ManagementDrawer`'s existing dismiss button already uses "Close" for the same kind of action, so this reuses that vocabulary instead of inventing a third word.

**Speaker portrait resolved in `App.tsx` via `actors.find(a => a.id === selectedActorId)`, not stored on `DialogueOverlayNode`/passed through the dialogue content itself.**
`DialogueNode.speaker` is just a display-name string, not an actor-id reference — there's no schema link from a dialogue node back to the Actor who's "in" it. Since `App.tsx` already tracks `selectedActorId` (set together with opening the dialogue in `handleSelectActor`) and already has the full `actors` array loaded, resolving the portrait there and passing a plain `speakerImageAsset?: string` down keeps `DialogueOverlay` decoupled from `src/content/` (same pattern as `NodeInteractionCanvas`'s `imageAsset` prop) without adding a new content-schema field. Doesn't yet handle a dialogue with more than one distinct speaker changing portrait mid-conversation — not needed by anything that exists today (Mara Venn's dialogue has one speaker throughout).

**`npm run lint` added as its own explicit step in `CONTRIBUTING.md`'s Definition of Done and the `verify-phase` skill, not left implicit under "CI covers it."**
CI failed on commit `a50473b` (`duel.ts`'s `player`/`opponent` declared `let` but only ever mutated via property assignment, never reassigned — `prefer-const`), even though that phase's own report showed a clean `tsc -b --noEmit` and a fully green `npm run test`. Root cause: neither `CONTRIBUTING.md`'s Definition of Done nor the `verify-phase` skill's checklist ever instructed running `npm run lint` locally — the skill's old step 6 only *mentioned* that CI runs lint, which reads as reassurance ("already covered") rather than an instruction to actually run it before declaring a phase done. `tsc` and ESLint check genuinely different things (this exact error isn't a type error), so passing one is no signal about the other. Both docs now list lint as its own required step, matching CI's actual order (type check → lint → test) exactly, so "phase done" locally means the same three checks CI will run, not two of three.

**`ManagementDrawer` takes an `items: Record<string, ItemDisplayData>` prop, not an `Item` (content type) import.**
`src/engine/` may never import `src/content/` directly (`web-implementation.md` §3) — `App.tsx` resolves the full `Item` content and maps it down to the three fields the component actually renders (`name`, `description`, `imageAsset`), the same content-derived-props pattern already used for `endeavorTitles` and `NodeInteractionCanvas`'s `actors` prop. `ItemDisplayData` is a plain interface owned by `ManagementDrawer.tsx` itself, not a re-export of the schema-inferred `Item` type.

**Character Roster's "met" status derived from `dialogueProgress[actor.dialogueId] !== undefined`, not a new `PlayerState` field.**
`dialogueProgress` already gets an entry the first time an actor's dialogue is opened (`handleSelectActor`, `App.tsx`) — it already means "has this dialogue been engaged," and checking its presence for a given actor's `dialogueId` doesn't change what it means to its other consumer (`resolveDialogueEntryNodeId`), it's just a second reader. Adding a dedicated `metActors: string[]` would have duplicated a fact the store already tracks. Filtering happens inside `ManagementDrawer` itself (a plain `usePlayerStore` read), not in `App.tsx`, mirroring the Inventory tab's existing split: content shape (the full roster, everyone, all fields) arrives as a prop; live save-state (which of them the player has actually met) is read directly from the store and combined at render time.

**Faction content loaded in `App.tsx` for the first time — `faction_city_watch.json`/`faction_wagering_ring.json` existed and were already schema-validated by `content-integrity.test.ts`, but nothing had ever read them into the running app.**
Needed to resolve an Actor's `factionIds` into a display name for the Roster. Loaded both existing faction files via the same explicit-per-file `loadContent` call already used for every other content type in `App.tsx`, rather than introducing `import.meta.glob`-based loading there — `App.tsx` has no glob-loading precedent (only the test files do), and mixing loading strategies within the same file would be a bigger, unrelated architecture change to bundle into a roster feature.

**Every field in a Roster row (portrait, name, title, faction line, description) renders conditionally, with no placeholder for a missing value — more defensive than `ActorSchema` currently requires.**
`name`/`title`/`description` aren't optional on `ActorSchema` today, so this is stricter than the schema demands. Requested explicitly (not inferred): future actors may end up thin on data, and an empty "Title: " or "Faction: " label reads worse than the line simply not being there. No schema change made — this is a rendering stance, not a new optionality contract.

**Reversed, partially: dialogue-open state moved into `PlayerState` (`activeDialogue`), overturning the earlier "actor-selection state... kept as local React state" decision — but only the visibility half.**
That earlier decision conflated two things under one description: which actor's button is highlighted (`selectedActorId`) and which dialogue is actually showing (previously `openDialogueId`). The first is genuinely view-only and stays local. The second stopped being local-state-appropriate the moment something other than a click needed to control it — an `EndeavorPhase.autoDialogueOnEnter` trigger or a minigame's `onSuccessCommands` can now open a dialogue with no actor ever clicked, and nothing in the command-dispatch pipeline (a pure function over `PlayerState`) can reach React component state. Same shape of problem `activeMinigame` already solved; `activeDialogue: { dialogueId: string } | null` mirrors it exactly, including persistence treatment (`extractPlayerState`). Two new commands, `COMMAND_OPEN_DIALOGUE`/`COMMAND_CLOSE_DIALOGUE`, control it — kept separate from `COMMAND_ENTER_DIALOGUE_NODE` (which stays pure bookkeeping, no side effects) for the same reason `COMMAND_ENTER_DIALOGUE_NODE` and `COMMAND_SELECT_DIALOGUE_CHOICE` were already kept apart; `COMMAND_CLOSE_DIALOGUE` mirrors `COMMAND_CANCEL_MINIGAME`'s unconditional, no-guard simplicity.

**Speaker-portrait resolution switched from `actors.find(a => a.id === selectedActorId)` to `actors.find(a => a.dialogueId === activeDialogue?.dialogueId)`.**
The `selectedActorId`-based lookup only worked because a click always set it right before opening a dialogue — a precondition that doesn't hold for an auto-triggered or minigame-triggered dialogue (nothing selected an actor). `Actor.dialogueId` is a stable, already-existing reverse link, so resolving the speaker through it is correct for every dialogue-open path, not just clicks — a genuine improvement enabled by the same `activeDialogue` migration, not a separate feature.

**`DialogueOverlay` drops its `onClose` prop entirely, following `MinigameOverlay`'s existing precedent — the Close button and the ending-choice branch dispatch `COMMAND_CLOSE_DIALOGUE` directly instead of calling a callback.** It still takes `node`/`speakerImageAsset` as content-derived props (unlike the fully prop-less `MinigameOverlay`) because only `App.tsx` has resolved dialogue/actor content to give it — the two components converge as far as the `src/engine/`-never-imports-`src/content/` boundary allows, not further.

**`computePoiEntryEffects`/`computeDistrictEntryEffects` extracted into a new `src/engine/utils/entryEffects.ts` rather than left inline in `App.tsx` as the feature spec's code sketch showed.**
`CONTRIBUTING.md`'s Definition of Done requires unit test coverage for non-trivial pure functions, and `App.tsx` has no test file — nothing there has ever been unit-tested, only reachability-verified. Extracting the pure computation half (not the dispatching executor, which genuinely needs `App()`'s scope) makes the new on-enter-effects logic testable the same way `evaluateDialogueRequirement` already is, without changing any behavior the spec described. District's entry-effects route through the same `EntryEffect[]` shape as POI's even though it can only ever produce a `SOUND` effect today (confirmed explicitly, not assumed) — one code path instead of two near-duplicates.

**Process retrospective, requested explicitly after the Duel/Item/Roster/Dialogue-visibility run of phases — four process gaps found and fixed, one tooling note documented.**
Reviewed the whole session's history (not a single phase) for recurring friction, the same way `feature-workflow.md` itself was originally derived from a retrospective rather than written speculatively. Four concrete gaps, each fixed at the process layer, not just patched for the one instance that exposed it:
1. **`npm run build` was never part of the local Definition of Done, but `.github/workflows/deploy.yml` (added this session) runs it on every push to `main`.** Exact same shape as the earlier lint gap — a CI-adjacent workflow checking something local verification didn't. Added as DoD step 4 / `verify-phase` step 4; `verify-phase`'s "Check CI coverage" step now reads both workflow files, not just `ci.yml`.
2. **The `resolveAssetUrl` base-URL bug (see this file, 2026-08-12, above) was invisible for the entire project history because nothing ever unit-tested a config-resolved path/URL, only the pre-resolution string.** Added explicit guidance to `feature-workflow.md`'s spec template "Environment notes" section: does this feature construct a URL/path from build/runtime config, and if so, is the *resolved* value under test?
3. **Component test boilerplate ripples every time a component gains a required prop** (`ManagementDrawer`'s prop list grew twice this session — `items`, then `roster` — touching ~15 existing render calls each time). `commands.test.ts` already solved this for `PlayerState` with `makeState(overrides)`; documented the same convention for component tests in `CONTRIBUTING.md` (a local default-props helper once a component has 3+ required props), to be adopted opportunistically rather than as a forced retrofit pass.
4. **`persistence.test.ts`'s hand-maintained persisted-keys list had no documentation explaining it's deliberate, not an oversight.** Added one sentence to `web-implementation.md` §6 stating the intent explicitly (forces a conscious persist/don't-persist decision on every new `PlayerState` field) so a future pass doesn't "fix" it into something automatic and quietly remove the guard.

Also documented (not a process gap, a tooling capability that changed mid-session): `gh` CLI is now installed and authenticated locally, noted in `CLAUDE.md` as preferred over hand-rolled `curl`/GitHub-API polling scripts (this session wrote several of the latter, e.g. to watch Pages-deploy status). The first draft of that note named the authenticated account and a local install path — caught immediately as inappropriate for a public repo (this one) and rewritten to state the capability without account-identifying or machine-identifying detail; `CLAUDE.md` now also states the public-repo/no-credentials rule explicitly so it isn't re-derived from scratch next time.

## 2026-08-13 (fifth follow-up: active/completed split in the Endeavors tab)

**Reused the exact same "no `nextPhaseOnSuccess` = terminal = complete" rule the notification system's Endeavor-completion effect already relies on (`web-implementation.md` §3/§10), rather than defining a second notion of "completed."** A new `phaseIsTerminal: Record<string, Record<string, boolean>>` prop (content-derived in `App.tsx`, same tier as `phaseObjectives`) lets `ManagementDrawer` split `activeEndeavors` into ongoing vs. completed without importing content itself. Deliberately not a new PlayerState field or command — "completed" was already fully derivable from existing content + existing store state, just not yet read anywhere for this purpose. Keeping it defined in exactly one place means the Endeavors tab's split can never disagree with what actually triggers a "Completed: ..." toast.

**Completed entries stay visible (dimmed, ✓-prefixed, below a divider), not hidden.** The request was specifically about *identifying* completed Endeavors so the player knows what to focus on next, not about decluttering the list — hiding them would remove a legitimate record of what's been resolved, which is exactly the kind of "quietly disappearing progress" this project's own asset-visibility principle (`game-design-spec.md` §10, "never fail silently") argues against by analogy, even though that principle is about assets specifically.

## 2026-08-13 (fourth follow-up: two small UX reports after the notification system shipped)

**Endeavors tab now renders `EndeavorPhase.objectiveText` instead of the raw `currentPhaseId`.** Reported directly: `phase_confront_the_buyer` reads as a bug, not content, to a player. The fix reuses an already-authored field (`objectiveText` exists on every phase specifically for this kind of player-facing prose, per `game-design-spec.md` §8) rather than inventing new display copy — `ManagementDrawer` gains a `phaseObjectives: Record<string, Record<string, string>>` prop (content-derived in `App.tsx`, same pattern as `endeavorTitles`), falling back to the raw id only if a phase is somehow missing objective text (defensive, not the expected path). `ManagementDrawer.test.tsx`'s prop list has now grown a third time (`items`, `roster`, `phaseObjectives`) — per `CONTRIBUTING.md`'s already-documented convention ("retrofit a local default-props helper the next time you're already touching the file for another reason"), this pass finally added `renderDrawer(overrides)`, collapsing ~20 repeated full-prop-list render calls to one line each.

**"Pay off the buyer" got a purely cosmetic click-acknowledgement, not a completion gate.** Explicitly asked and confirmed with the user before touching this: `web-implementation.md` §3 already documents the action as deliberately repeatable with no one-time gate ("the action has no persisted one-time gate, so it stays clickable... same category of accepted simplicity as the no-anti-grinding reputation gap") — reported confusion here was about the button giving no visible sign a click registered (the currency toast is real feedback, but easy to miss if you're not looking at the corner), not a request to change what paying actually does. Fixed with local-only state (`buyerJustPaid`, mirroring `selectedActorId`'s existing local-state precedent) that briefly swaps the label to "Paid ✓" and disables it for ~1.2s before reverting — no new command, no schema change, no change to the underlying repeatable behavior.

## 2026-08-13 (third follow-up: notification system)

**Currency/item/reputation notifications are derived by diffing `PlayerState` before vs. after every `dispatchCommand`, not by switching on the dispatched command's `type`.** The initial instinct (switch on `command.type`, reading `COMMAND_ADJUST_CURRENCY`'s/`COMMAND_ADD_ITEM`'s own payload directly — simpler, and avoids any before/after comparison) turned out to be wrong: `COMMAND_SELECT_DIALOGUE_CHOICE` and `COMMAND_RESOLVE_MINIGAME`'s handlers (`commands.ts`) apply their nested `commands`/`onSuccessCommands`/`onFailureCommands` via *direct recursive `applyCommand` calls*, never a second `dispatchCommand` call — the exact same reason `eventLog` itself only ever records the outer dispatched command, never what ran inside it (confirmed by inspection, not assumed). Since nearly every real currency/item/reputation change in this game happens exactly that way (a dialogue choice's consequence, a minigame's win/lose outcome), a type-switch design would have shipped a feature that silently never fired for the cases that actually matter, working only for the rare top-level-only dispatch. Diffing the final `PlayerState` against the pre-dispatch snapshot sidesteps this entirely — it reflects the cumulative effect regardless of dispatch depth, with zero changes to `commands.ts`. Caught during design, before any code was written, by re-reading `COMMAND_SELECT_DIALOGUE_CHOICE`'s handler specifically to check the assumption — worth recording since it would have been a genuinely hard bug to notice afterward (every existing test that exercises a top-level `COMMAND_ADJUST_CURRENCY` dispatch would still have passed).

**Endeavor-completion detection lives in `App.tsx`, not the store, and specifically needed a `useRef` seeded with the current value, not an empty one.** "Terminal phase" is a content fact (`EndeavorPhase.nextPhaseOnSuccess` being absent) that `src/engine/` may never read, so — unlike currency/item/reputation — it can't be derived inside `dispatchCommand`. The natural alternative, a `useEffect` reacting to `activeEndeavors` (the same shape of mechanism the phase-change dialogue-trigger effect already uses), has a real failure mode if the "previous value" comparison starts from nothing: `useEffect` always runs once after the very first render regardless of its dependency array's contents, so a save reloaded with an already-completed Endeavor would fire a false "Endeavor Completed!" toast on every page load. Seeding `useRef(activeEndeavors)` (its initializer form, capturing whatever's already loaded at mount) rather than `useRef({})`/`useRef(undefined)` makes the first comparison always be against itself, correctly suppressing that false positive while still catching every genuine in-session transition. Verified live (see Status below) specifically against this reload case, not just the happy path.

**`notifications` is store-only state, not part of `PlayerState`/`PlayerStateSchema` — same treatment as `eventLog`, deliberately not extended into save data.** A toast is inherently about "something just happened," which stops being true the moment a page reloads; persisting it (or worse, needing save-file migration logic for it later) would be modeling permanence for something the game design explicitly treats as transient (`game-design-spec.md` §12: "carries no save-file weight... not a log the player can review after the fact").

**Position/z-index settled directly by the user during scoping, not assumed:** floating above everything (`z-[100]`, above the existing `z-50` overlay tier) rather than picking a screen corner that dodges the Journal button — the request was explicitly for something that reads as "on top of whatever you're doing right now," not permanent, position-locked chrome. Reputation-change notifications (Actor/Faction) were also explicitly opted into during the same scoping conversation, alongside the originally-requested currency/item/Endeavor-completion set, since the identical diffing mechanism covers them for free.

## 2026-08-13 (second follow-up: duel comprehension + a real softlock)

**`DuelConfig` gained an optional `allowFlee` field, defaulting to `true`, rather than removing the Flee button from `DuelGame` outright or hardcoding an Endeavor-specific exception.** The report: fleeing "A Debt in Steel"'s duel permanently strands the Endeavor — nothing re-triggers `dialogue_the_offer` (its own `autoDialogueOnEnter` is scoped to `phase_the_offer`, and the phase has already advanced to `phase_the_duel` by the time Flee is clickable), so `COMMAND_CANCEL_MINIGAME` would leave the player stuck mid-phase with no path forward. This is real and was missed during "A Debt in Steel"'s own Reachability check — worth noting as a gap in that check's coverage (it verified the *forward* path, not that every exit from a minigame has a path forward too). Made it a `DuelConfig` field (not a global engine behavior change) because Flee is legitimate and worth keeping for any future duel a player *can* walk away from and re-attempt — this is a per-encounter authoring decision, not a property of the `DUEL` mechanic itself. Default `true` so every `DUEL` authored before this field existed (there is exactly one other, in `DuelGame.test.tsx`'s fixtures) keeps its current behavior unchanged. Not treated as a Feature/Engine spec of its own — a single optional, backward-compatible boolean fixing a proven softlock, following the same precedent as `startingDistance` (also added directly to `DuelConfig` without a separate spec cycle) — flagged here instead so the reasoning isn't silently assumed.

**Added a small, dependency-free `Tooltip` component rather than reaching for a UI library.** The duel screen's distance indicator, five actions, and Energy/Poise stats were all reported as incomprehensible to a first-time player with no explanation anywhere on screen. `package.json` has no tooltip/popover dependency and nothing elsewhere in the project renders a floating/positioned element, so this is a genuinely new small primitive, not a case of missing an existing pattern to reuse. Built CSS-only (Tailwind `group`/`group-hover`/`group-focus-within`, no portal, no positioning library) — the tooltip bubble is always mounted (visibility toggled via opacity), not conditionally rendered, which also makes it trivial to assert against in tests. Deliberately narrow in scope (a short label anchored above its trigger); reach for something heavier only if a future tooltip actually needs edge-of-screen repositioning or rich content.

**Energy/Poise labels keep their exact original text layout (`"Poise: {value}"`, contiguous) with the `(?)` tooltip trigger appended as a separate trailing element, not wrapping the label word itself.** `@testing-library`'s `getByText` computes an element's text from only its *direct* text-node children, not recursively through nested elements — wrapping "Poise" itself in a nested `<Tooltip>` would have silently broken `DuelGame.test.tsx`'s existing `getByText("Poise: 45")`-style assertions (the label text would move one level deeper, no longer a direct text-node child of the `<p>`). Appending the tooltip as a trailing sibling element keeps `"Poise: 45"` as the paragraph's own direct-text content, exactly as before.

## 2026-08-13 (follow-up, after real art was added for the three new Actors)

**Speaker-portrait resolution switched (again) — this time from `Actor.dialogueId` to `DialogueNode.speaker`/`Actor.name` matching.** Requested after real art was added for `actor_duro_vantry` and the reckoning-win/lose dialogues still showed no portrait for him. Root cause: the `dialogueId`-based match (itself a deliberate earlier decision, see the "Speaker-portrait resolution switched from `actors.find(a => a.id === selectedActorId)`..." entry above) only resolves an Actor's portrait in their *own* home dialogue — exactly the same shape of gap as `game-design-spec.md` Open Design Gap #14 (a single fixed `dialogueId` can't represent "this Actor also speaks in these other scenes"). Rather than solving Gap #14 itself (a real, separate, not-yet-scoped mechanism for varying which dialogue an Actor's *click* opens), portrait resolution was decoupled from it entirely: `DialogueNode.speaker` is already a free-text display string content authors keep in sync with the speaking Actor's name, so matching on that resolves a portrait correctly in every scene an Actor speaks in — their own dialogue, a scripted scene, or a minigame's reckoning dialogue — without needing to know which dialogue is "theirs."

**`DuelGame`'s log switched from interpolating the raw `DuelAction` enum (`DIRTY_TRICK`) to a shared `ACTION_LABELS` map (`Thrust`, `Parry & Riposte`, `Dirty Trick`, etc.), bolded per-mention in the rendered log line.** The enum value leaking into player-facing text was a real, reported readability issue — the label map already existed as a component-local list for the action buttons; moved it into `duel.ts` (the same module that builds the log strings) and had `DuelGame.tsx` import it for both the buttons and a small text-splitting helper that bolds any label substring in a log line, so button and log text can never drift into two different names for the same action. One test (`DuelGame.test.tsx`'s 7-turn Taunt-to-defeat loop) had to switch from `getByText("Taunt")` to `getByRole("button", {name: "Taunt"})`, since after the first turn resolves the log itself contains a bolded "Taunt" — `getByText` became genuinely ambiguous between the button and the log entry, not just newly strict.

**`DialogueOverlay` no longer auto-wraps `node.text` in curly quotes.** The wrapping assumed every dialogue node's text is a single unbroken line of spoken dialogue (true of `dialogue_mara_venn`, the only content that existed when it was added) — "A Debt in Steel"'s dialogues, carried over from the source draft's own literary style, routinely mix third-person narration ("He unrolls a strip of paper...") with the character's own quoted speech within the same node. Auto-wrapping that combination produced visibly doubled quotation marks, a real reported bug. Removing the wrap is a content-authoring-neutral engine fix: existing content that never included its own quotes (Mara Venn) is unaffected in substance, just no longer artificially quote-wrapped either.

## 2026-08-13

**"A Debt in Steel" (`docs/features/content_a_debt_in_steel.md`) built directly from a hand-authored draft (`docs/drafts/a_debt_in_steel_draft.md`), with every command shape the draft had flagged as unconfirmed corrected against the real engine.** `COMMAND_ADJUST_CURRENCY`'s payload was guessed as `{currency, amount}`; the real shape is `{denomination: "gold"|"silver"|"bronze", amount}`. `COMMAND_GRANT_ITEM` doesn't exist; corrected to `COMMAND_ADD_ITEM` `{itemId, quantity}`. `COMMAND_START_MINIGAME`'s payload was guessed as `{minigameType, opponentActorId, locationId}`; the real shape is the discriminated `MinigameLauncherPayload` (`{type: "DUEL", sourceId, config: DuelConfig, onSuccessCommands, onFailureCommands}`), with the win/lose reckoning dialogues opened via `onSuccessCommands`/`onFailureCommands` dispatching `COMMAND_ENTER_DIALOGUE_NODE` + `COMMAND_OPEN_DIALOGUE` — the first real use of a capability `feature_dialogue_visibility_and_auto_triggers.md` had explicitly called out as "gained for free" but never exercised. `minFactionReputation` was the one flagged item that turned out to already be correct as drafted.

**`dialogue_the_challenge`'s ending choice dispatches `COMMAND_ADVANCE_ENDEAVOR_PHASE`, not `COMMAND_START_ENDEAVOR` as the original draft had it.** The draft predates `Endeavor.autoStartOnEnter`; with it shipped, `COMMAND_START_ENDEAVOR` fires automatically on POI entry, so the scene's own ending choice only needs to advance the phase already-started endeavor forward.

**`actor_bookkeeper` authored as a real, durable, `isUnlocked: true` Actor with its own separate `dialogue_bookkeeper_default`, distinct from the `dialogue_the_challenge` scene he appears in — and the same split applied, narrower, to `actor_duro_vantry` (`dialogue_duro_vantry_default`).** Conflating "the one scripted scene an Actor appears in" with "what a direct click on them does" breaks the moment the Actor is reused with different relevant content, which is exactly the shape of problem the Bookkeeper (reusable across future endeavors) already has today. Logged as `game-design-spec.md` Open Design Gap #14 rather than solved — no phase-conditional dialogue-selection mechanism exists, and building one wasn't asked for. `actor_anselm_draye` was deliberately *not* given a separate default dialogue — his own `dialogueId` stays `dialogue_anselm_recruit`, reused, so a player who declines at `phase_the_second` can click him again to reconsider (an intentional off-ramp the source draft explicitly wanted, not enforced). This is accepted as an inconsistent-looking pattern across three Actors in the same content, not a mistake — the difference is whether "click again later" is a real, desired path (Anselm) or purely incidental (Bookkeeper, Vantry).

**`poi_widowmaker_alley` and `actor_duro_vantry` unlock together, at the end of `phase_the_second`; `actor_anselm_draye` unlocks at the end of `phase_the_challenge`.** Neither unlock point was specified by the source draft or the prior revision discussion — both are judgment calls made here, since leaving either Actor or the POI permanently `isUnlocked: false` with no unlock trigger anywhere would make them permanently unreachable, which the Reachability check would have caught regardless. Placed at the phase transition that narratively first makes each one "real" to the player (Anselm becomes a named participant once `phase_the_second` begins; Widowmaker Alley and Vantry become real once the player has committed to standing there).

**`EndeavorPhase.unlocksNodesOnComplete` (the content-level field) mirrored to match the corresponding `COMMAND_ADVANCE_ENDEAVOR_PHASE` payload's own `unlocksNodesOnComplete`, even though nothing in `commands.ts`/`entryEffects.ts` appears to read the content-level copy.** Confirmed by inspection, not assumed — only the payload-level value (authored on the triggering dialogue choice) is ever consulted at runtime. This is a pre-existing duplication already present in `endeavor_the_missing_broadsheet.json` (both copies kept as `[]` there too), not introduced by this content. Followed the existing precedent rather than diverging from it or silently fixing what might be a real but out-of-scope redundancy — noted in `content_a_debt_in_steel.md`'s Open Questions instead.

**Node-unlock rendering found to be silently non-functional for the project's entire history — `unlockedNodes` written correctly, read nowhere.**
Surfaced while scoping the entry-effects/`autoDialogueOnEnter` extension: asked whether actor visibility already respects `isUnlocked` before assuming a content-only fix, and traced the actual render sites instead of assuming the existing POI lock indicator (`WorldNavigationView`, `"??? (locked)"`) was wired to the dynamic `unlockedNodes` record. It isn't — it only ever read the static `Poi.isUnlocked` content field. Grepped every `unlockedNodes` reference project-wide to confirm: written in `commands.ts`, asserted in tests, never read by any component. This means `COMMAND_UNLOCK_NODE` and every `EndeavorPhase.unlocksNodesOnComplete` dispatched so far (in the one shipped endeavor) have had zero visible effect on anything a player could see — the mechanic was fully wired at the state layer and fully dead at the render layer. This is a pre-existing defect from well before this session, not something introduced by any recent phase — it just took the right question (asked while scoping unrelated new work) to surface it. Logged as `game-design-spec.md` Open Design Gap #13 and its own spec (`docs/features/feature_node_unlock_rendering.md`) rather than folded into the unrelated entry-effects work that prompted finding it — the fix (a shared `isNodeUnlocked` helper, a plain OR of the static and dynamic flags, since `unlockedNodes` is confirmed monotonic — nothing ever sets it back to false) is small, but the finding — "unlocking has never worked" — is significant enough to warrant its own record, not just a line item. Fixing the `WorldNavigationView` POI check changes real, already-shipped behavior (a POI authored locked-then-unlocked would previously have stayed permanently shown as locked); flagged explicitly as a behavior fix, not bundled silently into the surrounding refactor.

## 2026-08-22

**Three independently-invented trigger fields (`entrySoundAsset`, `autoDialogueOnEnter`, `autoStartOnEnter`) unified into two schema fragments, not one, against the original request's suggested single shape.** The prompt proposed one `TriggerableSchema` (`onEnter?: EntryEffect[]`) for all four content types (District, POI, EndeavorPhase, Endeavor). Confirmed via code inspection that `EndeavorPhase`/`Endeavor`'s trigger objects are byte-identical `.strict()` shapes (a real, concrete duplication) — but District/POI's trigger condition is implicit ("this node itself was entered") while EndeavorPhase/Endeavor's is targeted ("a specific, different POI was entered while this phase/endeavor is in the relevant runtime state"), since a phase/endeavor isn't a spatial node the player can "enter." Forcing one identical shape across both would have either lost the targeting information or bolted an unused `poiId` onto District/POI. Went with two fragments sharing the same `EntryEffect[]` payload vocabulary and the same executor (`TriggerableSchema` for District/POI, `PoiEntryTriggerSchema` for `EndeavorPhase.onPoiEnter`/`Endeavor.onPoiEnter`) — the real unification target (payload shape, dispatch mechanics, duplicate-shape removal) without a false uniformity.

**`Endeavor.onPoiEnter.onEnter` deliberately excludes an authorable `START_ENDEAVOR` effect — the engine always synthesizes it from the parent `Endeavor`'s own `id`/`initialPhaseId`.** The old `autoStartOnEnter` trigger object never carried `endeavorId`/`initialPhaseId` either — `computePoiEntryEffects` always pulled them from the enclosing `Endeavor`. Considered letting content author the full `EntryEffect[]` including `START_ENDEAVOR` directly, for a more literally "generic" schema — rejected: it would force an author to restate `endeavorId`/`initialPhaseId` that already exist as that same file's own `id`/`initialPhaseId` fields, a needless second source of truth with no present benefit (nothing needs more than one `START_ENDEAVOR`-shaped effect per endeavor). A deliberate partial generalization, not a silently-forced uniform one.

**`onExit?: EntryEffect[]` considered and explicitly not built.** Nothing in the engine fires on leaving a location today — this would be new capability, not a retrofit of existing behavior. This project already has a documented near-miss on exactly this shape of mistake: `feature_audio_system.md` records `entrySoundAsset` almost being wired to the existing `onLeave` call site, caught in review because that call site only ever means "left a POI back into the same district" (no real district-to-district travel exists), not a generic "left this location" moment. Building `onExit` now, with zero real trigger instances needing it, would either sit unused or invite that same wrong wiring a second time. `TriggerableSchema`'s shape doesn't foreclose adding it later, non-breaking, once a real need and a correct trigger moment both exist.

**`EntryEffect`'s canonical definition (type + new `EntryEffectSchema` Zod mirror) moved from `src/engine/utils/entryEffects.ts` into `src/engine/types/index.ts`.** It was content-authorable indirectly before (via the three ad-hoc trigger fields resolving into it), but had no Zod schema of its own — fine while purely an internal computed value. Once `TriggerableSchema`/`PoiEntryTriggerSchema` needed to validate it directly inside content JSON, it needed the same treatment as `StateCommandSchema`/`MinigameLauncherPayloadSchema`: a `.strict()` discriminated-union Zod mirror living alongside the other shared engine/content vocabulary in `engine/types/`, per the boundary rule `docs/engine.md` §5 already documents. `entryEffects.ts` re-exports it so `App.tsx`'s existing import needed no change.

**Migration verified via a full manual playthrough (scripted Playwright pass against the real dev server), not just passing unit tests.** Requested explicitly, given this is a behavior-preserving refactor of three already-shipped, real features — the actual risk is silent wiring drift the type checker and existing unit tests (which were themselves being rewritten as part of this change) wouldn't catch. Walked the entire "A Debt in Steel" auto-trigger chain from a fresh load: district-mount `SOUND`, POI-entry `SOUND` + `START_ENDEAVOR` + `DIALOGUE` (the challenge), the same-POI phase-change re-trigger (the second), a confirmed non-fire on a POI-mismatched target (arrival phase not firing at the tavern), a fresh-POI-entry trigger (arrival), and a second same-POI phase-change re-trigger (the offer) — matching pre-migration behavior exactly, with screenshots and captured console/network evidence, not just "tests pass."

**`simple-english` (ASD-STE100) skill vendored into `.claude/skills/`, scoped by prose only — not by a technically enforced mechanism.** Checked rather than assumed: Claude Code's own skill-development reference documentation defines `SKILL.md` frontmatter as exactly `name`/`description`/optional `version`/`license`/`compatibility`/`metadata` — no field restricts a skill to specific file paths, and plugin manifests have no such field either. The one real enforced, path-based mechanism is a `PreToolUse` hook, but it fires on every matching tool call project-wide and can't be conditioned on "only when this skill is active" — a hook hard-blocking `src/content/**` or code files would also block this project's normal narrative-content authoring (the `endeavor-content` skill's whole purpose) and all normal code work. Given that constraint, confirmed with the user directly rather than picked silently: scoping is prose-only — a narrowed `description` field plus an explicit, honestly-labeled "Project Scope" section inside the skill file itself, naming exactly what's in scope (`docs/`, `CONTRIBUTING.md`, `CHANGELOG.md`, `docs/decisions.md`, commit messages, PR descriptions) and what's excluded (`src/content/`, narrative prose discussion, code, code comments). Vendored (not installed via the plugin/marketplace system) specifically because the scope needed to be project-specific, which a generic marketplace install can't express without forking anyway. Vendoring also freezes this copy at today's upstream version (`AminBlg/SimpleEnglish` v1.3.0, MIT) — future upstream rule refinements won't reach this project automatically, the real cost of the scoping customization this required.

## 2026-08-29

**ECS evaluated and declined; modifier system adopted as the missing third primitive.** ECS was assessed against the existing CQRS/trigger/effect architecture and rejected for adoption: its performance benefits require large homogeneous entity populations this game doesn't have, and its mutable-arena storage model is structurally opposed to `PlayerState` being an immutable, Zod-validated, persisted value — adoption would degrade `persistence.test.ts`'s deliberate key list, `PlayerStateSchema`, `diffForNotifications`, and put pressure on the engine ↔ content boundary. Three ideas were borrowed: composable capability schema fragments (`TriggerableSchema`, now `ModifierSourceSchema`), entity-as-id sparse side tables (`unlockedNodes`, now `equipped` — deferred, see below), and — the one that motivated the evaluation — query-time aggregation of a value from several independent sources, with the discipline that the aggregate is *never stored*, only computed at the point of use. The evaluation reframed the architecture as Clausewitz's three primitives — triggers (`DialogueRequirement`), effects (`StateCommand`/`EntryEffect`), and modifiers — of which the third was missing. Modifiers are deliberately **not** modelled as trigger-gated effects: an effect would require derived stat state in `PlayerState` plus a correct inverse on unequip, whereas query-time aggregation keeps the save file free of derived values.

**Stellaris supplies the skeleton, Path of Exile supplies the arithmetic.** Named keys, condition-gated sources, and query-time aggregation come from Stellaris; the stacking formula (`(base + ΣFLAT) × (1 + ΣPERCENT)`, clamped at zero, rounded once) comes from ARPG conventions. Stellaris' duration/decay model was rejected (the world clock is discrete shifts, not a continuous tick) and ARPG affix rolling was rejected (hand-authored content, not generated). Modifiers are applied **inside the command handler**, with the modifier key derived from command type and the sign of the amount (`CURRENCY_GAIN` vs. `CURRENCY_LOSS`), scaling the bronze-equivalent magnitude before the sign is reapplied — in bronze rather than the payload's own denomination, because rounding in a coarse unit destroys any percentage smaller than that unit. Because the key is derived rather than authored, existing content JSON needed no changes to accommodate this — confirmed directly: the four existing `COMMAND_ADJUST_CURRENCY`/`COMMAND_ADJUST_REPUTATION` dispatch sites in dialogue content all pass non-zero `amount` with no `modifierKey`, so they inherit sign-derived defaults and are inert with an empty `ModifierSet`. `applyModifiers` clamps its result at zero, so a modifier can reduce a value to nothing but never invert it — without that clamp, a large negative `FLAT` exceeding the base would flip a currency loss into a gain once the sign is reapplied; this is the real, previously-undetected arithmetic gap the widened clamp closes, not a pre-existing shipped bug (the whole modifier system is new code, so nothing was inverting anyone's currency before this). Targeting is exclusive: a modifier scoped to one faction never appears in an unscoped query, chosen because the opposite reading fails silently. One limitation is accepted and documented rather than fixed: modifiers resolve as of the start of a dispatch, so an item granted and used within the same command batch does not modify that batch, and a `+5% currency` item cannot inflate a *cost*.

**The dice minigame's gambling carve-out: an optional `modifierKey` payload override, not a new command.** Dice is exactly EV-neutral (18 of 36 outcomes are even), so an unqualified `CURRENCY_GAIN` bonus item would make every throw positive-EV and, given no cooldown exists on repeated throws, an unbounded money press. `COMMAND_ADJUST_CURRENCY.modifierKey?: string` lets the dice launcher tag its payouts `CURRENCY_GAMBLING_WINNINGS` — deliberately **not** a member of `MODIFIER_KEYS`, so no item can ever be authored against it and it's inert by construction, not by convention. A cooldown would bound the *rate* but not restore the EV invariant, so the two are orthogonal; this carve-out was chosen to preserve the dice minigame's "transparent odds, legible at a glance" design intent, which an item silently shifting the odds would defeat.

**Equipping was deliberately deferred, not forgotten.** The play-style goal that motivated it (items with differing bonus *shapes*, not just bigger numbers) is delivered by the modifier layer itself; equip slots are only the selection mechanism on top. Because every part of that work is additive — an optional `slot` field, a defaulted `PlayerState` key, a second branch in `collectActiveModifiers` — deferring costs a few JSON edits later, whereas building early would commit the save format to a shape no content had yet exercised. Revisit trigger: the first time a second item competes for the same role. Two gaps were logged for it: the duel's absent per-action resource, and the equip-slot spec itself. Only `DUEL_DAMAGE_DEALT` is wired to duel damage this phase (every hit the *player* lands, including a successful Parry & Riposte's counter-damage) — `DUEL_DAMAGE_TAKEN`/`DUEL_STARTING_POISE` exist in `MODIFIER_KEYS` but stay unwired, since no worked example or item needs them yet and wiring an unused key ahead of a real need was judged premature, the same reasoning `onExit` was rejected under in the 2026-08-22 entry above.

**Reachability check surfaced that `importSave` — assumed to be an existing shippable UI feature when the implementation plan was written — has no UI entry point anywhere in the app.** It exists on the store (`playerStore.ts`) and is unit-tested, but nothing renders a file input or button that calls it; the plan's proposed verification method (craft a save, "Import" it, play the real duel trigger) didn't actually exist as a user-facing action. Corrected during the reachability check itself rather than left as a false claim: seeded `localStorage`'s `broadsheet_rapier_player_state` key directly (the same key/shape the `persist` middleware reads) with a `PlayerState` owning `item_duellists_rapier`, then drove the real app in a headless browser through "A Debt in Steel"'s real duel trigger. Result matched expectations exactly: the player's Thrust dealt 18 energy (15 base + 3 flat) instead of the baseline 15, while the opponent's own Thrust against the player stayed at 15 in both runs — confirming `DUEL_DAMAGE_DEALT` only affects damage the player deals, with zero console errors. See `docs/features/feature_modifier_system.md`'s Reachability section for the full script/output.

## 2026-08-29 (localization)

**Four architectural forks decided by the user before implementation started, not defaulted silently.** (1) Content translations stored as sibling **overlay files** (`actor_mara_venn.pt-BR.json` next to `actor_mara_venn.json`), not inline per-field locale maps or a separate translation table — existing English content stays completely untouched, and translating is incremental (a missing overlay falls back to English, not an error). (2) Engine/UI strings via **i18next + react-i18next**, not a hand-rolled `t()` — the real reason, stated explicitly so this isn't misread later as "chose it because it's the standard": correct pluralization (Portuguese's `_one`/`_other` split) and, for a language like Spanish that's likely to follow, grammatical gender agreement are well-solved problems in a mature library, and hand-rolling that logic even for one language is a real, specific reinvention risk this project chose not to take on. (3) The language dropdown lives in a **new header/HUD-level control**, not a `ManagementDrawer` tab — a language preference is a fundamental, always-reachable setting, not case-file content one click deeper behind the Journal. (4) Locale is a **second, independent Zustand `persist` store** (`useLocaleStore`, its own `broadsheet_rapier_locale` key), never a `PlayerState` field — importing someone else's save must never silently change your UI language, and this way `PlayerStateSchema`/`persistence.test.ts` needed zero changes.

**Only the rendered label of an enum localizes; the underlying identifier never does, for any enum, not just the one (`DuelAction`) that already had a label map.** `Shift`/`Season`/`Weather`/`DistanceState`/`MinigameType` values, `StateCommand` payloads, `DialogueRequirement.allowedShifts`, every schema enum, every future content author's authored JSON — all stay fixed, English, ASCII, locale-invariant forever. `t()` sits strictly at the render boundary (`t('enums.shift.MORNING')`); it never appears inside comparison/branching logic or a persisted value. Stated explicitly, not left implicit, because this is exactly the kind of boundary that's easy to blur by accident once translation touches almost every component.

**Overlay identity is resolved from the filename, not an internal `id` field** — none of the four overlay schemas (`BaseNodeTranslatableSchema`, `ActorTranslatableSchema`, `EndeavorTranslatableSchema`, `DialogueTranslatableSchema`) carry one. `actor_mara_venn.pt-BR.json`'s target is derived by stripping the `.pt-BR.json` suffix from its own filename. No redundant id to drift out of sync; `content-integrity.test.ts`'s new overlay referential-integrity check fails loudly if a base id extracted this way doesn't match a real canonical file.

**`'en'` is the deliberate default, with no `navigator.language` auto-detection.** Considered and rejected, not merely i18next's out-of-the-box behavior left unconsidered: the brief explicitly named English as default, and auto-detecting from the browser would silently switch a Brazilian Portuguese visitor to `pt-BR` on first load — surprising, and inconsistent with the dropdown being the one source of truth for locale selection.

**Personal names are deliberately never translated.** An Actor's `name` and a `DialogueNode.speaker` referring to the same Actor should be omitted from a locale overlay (falling back to the canonical English value in every locale), because `App.tsx`'s speaker-portrait resolution matches `DialogueNode.speaker` against `Actor.name` by exact string equality — translating one but not the other would silently break that match. `actor_mara_venn.pt-BR.json`/`dialogue_mara_venn.pt-BR.json` both omit `name`/`speaker` for exactly this reason.

**The vertical-slice content wiring turned out to require restructuring `App.tsx` more than the plan anticipated.** The plan's phrasing implied resolving a locale-merged object once and using it; in practice, every module-scope aggregate that could contain translated text (`pois`, `actors`, `factions`, `dialogues`, `rosterEntries`, `actorNames`, `factionNames`) had to move from a one-time module-load computation into a `useMemo` keyed on the locale store's value, cascading through several derived structures. This was necessary, not scope creep: the reachability check's own stated requirement (switching locale while a dialogue is already open must update its displayed text live) is impossible to satisfy with content resolved once at module load, since React never re-renders from a change to a plain module-level `const`. Confirmed working via a real headless-browser pass — see `docs/features/feature_localization.md`'s Reachability section.

**`LanguageSelector` mounted inside `WorldClockHud` rather than as a separate `App.tsx`-level sibling.** The plan said "near `WorldClockHud`"; `WorldClockHud` already *is* the header/HUD-level component the user chose for the selector, so nesting it there avoids a redundant prop/wiring layer while satisfying the same placement decision.

**Currency formatting's three independent implementations were unified as part of this work, not treated as pre-existing scope to leave alone.** `WorldClockHud`'s abbreviated display, `DiceGame`'s abbreviated display, and `notifications.ts`'s `formatCurrencyDelta` each spelled out "Gold"/"Silver"/"Bronze" (or their abbreviations) independently — translating each three separate times risked them drifting into three different Portuguese phrasings for the same concept. Consolidated into `src/engine/i18n/formatCurrency.ts`'s `formatCurrencyAbbreviated`/`formatCurrencyDelta`, the latter now genuinely testable against real Portuguese plural forms (a concrete before/after: "+3 Gold" → "+3 Gold Coins" / "+3 Moedas de Ouro") — a real, intentional wording change to already-shipped notification text, called out explicitly (see CHANGELOG's Changed section) rather than folded silently into the localization feature.

## 2026-08-29 (localization, full content pass)

**A real bug: `App.tsx`'s locale resolution was a hardcoded six-entity list, and Endeavors/Items were never on it.** Requested by the user to translate the remaining ~20 content files so the feature could actually be tested end-to-end. Authoring `endeavor_a_debt_in_steel.pt-BR.json`/`endeavor_the_missing_broadsheet.pt-BR.json` and the five item overlays revealed that neither content type was ever wired into `App.tsx`'s localization block at all — the overlay files validated correctly against `content-integrity.test.ts` (proving the schema/merge layer worked), but `endeavorTitles`/`phaseObjectives`/`localizedItemsById`-equivalent lookups didn't exist yet, so the Endeavors tab and Inventory tab stayed English regardless of locale. Not caught by any test, because no test exercised `App.tsx`'s actual wiring against a real overlay file for those two content types — the gap was only visible once real translated content existed and was clicked through in a real browser. This is exactly the category of risk `docs/feature-workflow.md`'s Reachability check (stage 8) exists to catch, and exactly why "verified via a real headless-browser pass" was the standard applied to the original vertical slice too — the coverage gap was in *scope* (two content types never wired), not in the depth of verification applied to what was wired.

**Fixed by generalizing the mechanism, not by extending the hardcoded list to eight entities instead of six.** `src/contentLocalization.ts`'s `loadLocalizedContent` was split into a new `applyLocaleOverlay(canonical, overlaySchema, overlayRaw, label, merge)` — the overlay-parse-and-merge half only, operating on an already-parsed canonical value — plus `loadLocalizedContent` itself, now a thin wrapper combining `loadContent` + `applyLocaleOverlay` (kept for `contentLocalization.test.ts` and any future caller without a pre-parsed canonical). `App.tsx` now maps `applyLocaleOverlay` generically over every canonical content array (`pois`, `actors`, `factions`, `itemList`, `endeavors`, `dialogueList`, plus `settlement`/`district` individually) inside `useMemo`s keyed on `locale`. A new overlay file for any existing content id is picked up automatically — no `App.tsx` code change needed, closing off this entire class of "forgot to wire a content type" bug for good, not just for the two types that happened to be caught this time.

**All 26 content files now have a `pt-BR` overlay** — settlements (1), districts (1), POIs (2), actors (4), factions (2), endeavors (2), dialogues (9), items (5). Personal proper names (Duro Vantry, Anselm Draye, Mara Venn) stay untranslated per the existing rule; `actor_bookkeeper`'s `name` ("The Bookkeeper") was translated to "O Contador" since it's a descriptive title rather than a proper name, with the identical string applied to every dialogue node where that Actor speaks (`dialogue_bookkeeper_default`, `dialogue_the_challenge`) to keep the speaker-portrait exact-match working. Place names (Lantern Ward, the Crooked Hour, Widowmaker Alley) were translated for immersion, consistent with the original vertical slice's precedent. Verified via a second real headless-browser pass exercising specifically the previously-broken content (Endeavor titles/objectives, all five items) and the trickiest name-translation case (the Bookkeeper) — see `docs/features/feature_localization.md`'s Reachability section.

**Content-authoring process updated: new content going forward should ship a `pt-BR` overlay at authoring time, not as separate follow-on work.** Requested by the user, given the process gap this phase exposed (translation work being treated as separable, deferrable follow-on is exactly how two content types went unwired for a full phase without anyone noticing). Scoped deliberately to *new* content only — not retroactive, not blocking anything already shipped — added to `docs/feature-workflow.md`'s Content/Adventure spec template and `CONTRIBUTING.md`.

**`LanguageSelector`'s flag icon took three iterations, each caught by actually testing the previous one rather than by inspection.** (1) Emoji inside a native `<option>` — rendered as plain "US"/"BR" text on Windows, in every browser, on every Windows version, because Windows' bundled emoji font has never shipped colored flag glyphs; this is a real, long-standing platform limitation, not a one-off rendering bug. (2) An inline SVG (`FlagIcon.tsx`) placed on the closed control only, styled to look like part of one unified control (border/background moved onto the wrapping `<div>`, the `<select>` itself made transparent) — fixed the rendering, but a native `<option>` can only ever hold plain text, so the flag still couldn't appear inside the open dropdown list, only on the closed selection. (3) Replaced `<select>`/`<option>` entirely with a custom button+listbox (`role="listbox"`/`role="option"`, closes on Escape or an outside click) so a flag renders next to every option, open or closed — the deliberate trade-off being the loss of the native `<select>`'s built-in platform behavior for two options that don't otherwise need it. A real implementation bug surfaced along the way: an earlier draft put `role="option"` on a `<li>` wrapping a separately-clickable `<button>` — a click dispatched on the `<li>` never reached the button's handler, since click events bubble up to ancestors, never down to descendants. Fixed by putting `role="option"` directly on the clickable `<button>` itself.

## 2026-09-03

**Six real gaps surfaced by recent work, all filed as backlog issues (#5-#10) with detailed descriptions, not just noted in conversation.** Requested by the user explicitly ("create GitHub issues... so later we can just trigger the implementation workflow") — each issue is written to be picked up cold, with enough context that a future session doesn't need to re-derive it: #5 CI never runs `npm run build` (a real blind spot — Vite's own build just started warning about bundle size after i18next landed, and nothing in CI will ever see that or a future hard failure); #6 `docs/execution-plan.md` is stale (still describes "Phase 0... not yet started" despite everything shipped since); #7 `DialogueNode.speaker`/`Actor.name` matching by string equality is fragile (caused real friction during localization, no automated check catches drift); #8 `importSave` has no UI entry point despite being fully built and tested; #9 i18next `t()` keys have no compile-time type safety; #10 (below) the branching-vs-practice conflict discovered while writing this entry's own triggering work.

**The GitHub Project board ("Broadsheet & Rapier Backlog", project #2) had no automation adding issues to it, and two real issues (#3, #4) had silently never been added.** Checked rather than assumed: `.github/workflows/` has no project-linking automation (only issue auto-assignment), so `gh issue create` alone never surfaced work on the board despite `docs/feature-workflow.md` treating "an existing Backlog issue" and "the Project board's Backlog column" as synonymous. Fixed by adding #3/#4 retroactively (Status: Done, since that work already shipped) and #5-#10 as new items (Status: Backlog) — but this is a manual fix, not a structural one; the underlying gap (nothing auto-adds a newly-created issue to the board) is still open and worth its own future fix if it keeps recurring.

**A `work-next-issue` skill was added to autonomously pick and fully drive one backlog issue through `docs/feature-workflow.md`, end to end — including the final push and issue close, deliberately not gated on a human confirmation step.** Requested by the user ("start to work on it autonomously"). The design choice that matters: every other skill in this repo (`verify-phase`, `design-review`) assists a human-supervised session; this one is built to run unattended (e.g. via a scheduled agent), so it can't have an "ask before the last step" gate without defeating its stated purpose — the safety instead comes from hard stops earlier (a dirty working tree, a Definition-of-Done failure, a genuine domain-rule ambiguity per `CLAUDE.md` §1) that halt the run and report rather than guess or ship broken work. Reuses `verify-phase` and `design-review` by invoking them, not by duplicating their content — same "three instances generalizes a pattern" discipline this project already applies to code (`dialogueResolution.ts`/`notificationResolution.ts`/`contentLocalization.ts`).

**Discovered while writing the skill: `CONTRIBUTING.md`'s branching rule ("branch as `phase/<n>-<name>`... then merge to `main`") has never actually been followed — every phase in this project's history, without exception, pushed directly to `main`.** The skill needed a concrete answer for its own commit/push step and the written doc and the real, revealed practice disagreed. Filed as issue #10 rather than silently picking one; the skill itself defaults to matching actual practice (direct-to-`main`, gated on a clean Definition of Done) since that's the standard actually in force, but says so explicitly in its own file header so the choice isn't buried, and is written to be easy to flip if #10 resolves the other way.

**Issue #7 (`DialogueNode.speaker`/`Actor.name` string matching) resolved by adding `speakerActorId?: string`, migrating all real content immediately rather than incrementally, and keeping the name-match path permanently for narration-only nodes.** The issue's own open question — does `speakerActorId` ever become required, or does the name-match stay a permanent parallel path forever — was answered both ways depending on the node: required (enforced via `docs/feature-workflow.md`'s template, not the schema, since a hard schema requirement would break `"speaker": "Narration"`-style nodes that have no Actor at all) for any new node whose speaker is a real Actor; permanent fallback for one that isn't. This is an implementation-detail/schema-design choice, not a `game-design-spec.md` domain rule, so it was fine to decide directly rather than flag as a hard stop. Also decided: migrate all 7 existing Actor-speaking dialogue nodes now rather than leave them as the issue's suggested incremental follow-on — small and mechanical enough (a one-line table lookup per file) that leaving it as a known gap would have cost more in tracking than just doing it. Verified via a real dev-server pass: entering the Crooked Hour auto-triggers `dialogue_the_challenge`, whose Bookkeeper portrait now resolves through `speakerActorId` (`actor_bookkeeper`) rather than the name match, rendering identically to before.

**Issue #6 (`docs/execution-plan.md` stale) resolved as rewrite-and-keep-live, not retire-or-merge.** The issue flagged both as genuinely open. Retiring it outright would have broken the several places that treat it as authoritative for the scaffold's history and lost real detail (the six phases' original scope/rationale) with no equivalent replacement; `docs/features/README.md` indexes *post*-scaffold work only, it was never meant to describe Phase 0 itself. Rewrote it instead: status line and all six phase headers now say "Complete," a new "What Happened After Phase 6" section explicitly hands governance of all current/future work to `docs/feature-workflow.md`, and the Working Agreement section is reframed as historical rather than live process. `CLAUDE.md` §2 was also corrected in the same pass — it asserted "Phase 0... only" as the project's *current* scope, which was simply false (minigames, dialogue, the modifier system, and localization have all shipped); rewritten to state the scaffold is complete and cite `docs/feature-workflow.md`/`game-design-spec.md`'s open gaps as the live scope boundary instead, without weakening the underlying "don't invent domain rules" rule. `CONTRIBUTING.md` step 2 and `README.md`'s doc-list line updated to match — both previously told a reader to check `execution-plan.md` for "the current phase," which no longer exists as a concept.

**A new foundational rule added to `game-design-spec.md`: no character-power progression, ever — `§7. Progression Philosophy`.** Requested by the user directly, as a decided principle (not an Open Design Gap — those track undefined mechanics, this is a settled constraint on what any future mechanic is allowed to do). The reasoning: this project's own primary narrative influence, Fafhrd and the Gray Mouser (`docs/narrative-inspirations.md`), is defined by protagonists who accumulate story, reputation, and possessions — often *losing* the latter — without ever becoming numerically stronger; a future minigame-difficulty-easing stat, a gear-tier system, or a village-management system that sells power would contradict that influence structurally, not just tonally. Three legitimate progression forms are named instead — world/state unlocking (§6, already implemented), item *variety* without item *power* (§9/Open Design Gap #11, already the case for `item_rapier`), and collection/management systems that cash out only as ACCESS or flavor, never as numeric power. Open Design Gap #2 (reputation tiers) gained a clarifying line tying it to the same rule, since a tiered-reputation system was the most likely place a future implementer might accidentally introduce a stat-like discount-stacking-into-power mechanic. Inserted as new §7, renumbering every section from the old §7 onward (Faction & Reputation through Localization, now §8–§14) — every cross-reference to a renumbered section was grepped for and updated across `game-design-spec.md` itself (the Open Design Gaps list), `web-implementation.md`, and every `docs/features/*.md` spec that cited one (`feature_modifier_system.md`, `feature_notification_system.md`, `content_a_debt_in_steel.md`, `feature_rapier_duel.md`, `feature_audio_system.md`, `docs/features/README.md`'s index). Deliberately did **not** renumber section citations inside `docs/decisions.md`, `CHANGELOG.md`, or `docs/execution-plan.md` — those are append-only historical records of what was true when each entry was written, and retroactively rewriting them to match current numbering would misrepresent history the same way editing a past decisions.md entry's reasoning would. One resulting wrinkle, left as-is rather than "fixed": `feature_notification_system.md`'s Status section quotes its own original `CHANGELOG.md` entry text, which still says the old `§12` — updating the spec's citation to `§13` while the quoted `CHANGELOG.md` text itself stays `§12` (correctly, per the rule above) means that one quote no longer verbatim-matches its source; a comment was added at that spot explaining why, rather than silently letting the two drift with no explanation.